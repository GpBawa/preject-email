============
Installation
============

At the command line::

    $ pip install Project Email

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Project Email
    $ pip install Project Email
